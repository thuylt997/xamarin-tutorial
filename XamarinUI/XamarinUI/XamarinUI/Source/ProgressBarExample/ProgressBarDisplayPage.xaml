<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:XamarinUI.Source.ProgressBarExample"
             mc:Ignorable="d"
             x:Class="XamarinUI.Source.ProgressBarExample.ProgressBarDisplayPage"
             Title="ProgressBar Example"
             Padding="10">

    <TableView Intent="Menu">
        <TableRoot>
            <TableSection>
                <TextCell Text="ProgressBar XAML"
                          Detail="Implement a ProgressBar in XAML"
                          Command="{Binding NavigateCommand}"
                          CommandParameter="{x:Type local:ProgressBarXamlPage}" />

                <TextCell Text="ProgresBar Code"
                          Detail="Implement a ProgressBar in code"
                          Command="{Binding NavigateCommand}"
                          CommandParameter="{x:Type local:ProgressBarCodePage}" />
            </TableSection>
        </TableRoot>
    </TableView>
</ContentPage>